<% 
=begin %>
<div class="container">
  <h1>Task List</h1>
    <ul>
      <% @tasks.each do |task| %>
        <li><input type="checkbox" disabled <%= "checked" if task.completed %>>
          <%= link_to task.name, task_path(task) %></li>
          <%= link_to "Delete", task_path(task), method: :delete, data: {confirm: "Are you sure?"} %>
          <i class="far fa-trash-alt"></i>
      <% end %>
    </ul>
    <p> <%= link_to "+ Add a new task" , tasks_new_path  %> </p>
</div>
<%
=end %>

<h1>My tasks</h1>

<ul id="tasks">
  <% @tasks.each do |task| %>
    <li>
      <% if task.completed %>
        <i class="far fa-check-square"></i>
      <% else %>
        <i class="far fa-square"></i>
      <% end %>
      <%= link_to task.name, task_path(task.id) %>
      ·
      <%= link_to task_path(task.id), method: :delete, data: { confirm: "Delete #{task.name}?" } do %>
        <i class="far fa-trash-alt" aria-hidden="true"></i>
      <% end %>
    </li>
  <% end %>
</ul>

<hr>

<%= link_to tasks_new_path do %>
  <i class="fa fa-plus"></i> Add a new task
<% end %>